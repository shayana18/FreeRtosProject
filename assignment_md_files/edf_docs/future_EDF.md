# EDF future improvements

- Add more automated checking for GPIO traces so EDF schedules can be compared against expected task IDs without relying only on manual waveform inspection.
- Add more edge-case constrained-deadline test cases to exercise the processor-demand admission path near rejection boundaries.
- Expand deletion and cleanup tests so EDF registry-list removal is covered more directly.
